export type Theme = {
    colors: {
        grayColor: string,
        lightColor: string,
        primaryColor: string,
        secondaryColor: string,
        terciaryColor: string,
    },
    fonts: {
        fontBold: string,
        fontMedium: string,
        fontPrimary: string
    }
};